apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sharks-monitor
  namespace: monitoring
  labels:
    release: prometheus 
spec:
  selector:
    matchLabels:
      app: sharks 
  namespaceSelector:
    matchNames:
      - monitoring
  endpoints:
  - port: app-metrics
    interval: 30s
    path: /metrics
